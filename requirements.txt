# WeMai 项目依赖

# 微信自动化相关
wx4py>=0.2.1
Pillow>=10.0.0
pyperclip>=1.9.0

# WebSocket 支持（maim_message 依赖）
websockets>=12.0

# MaiBot通信相关
# 本项目严格使用 0.6.8 的 Legacy Router/MessageBase/Seg API。
# 0.7.x 未经 Legacy API 兼容性验证，不自动升级。
maim_message==0.6.8

# 工具
aiofiles>=23.0
python-dotenv>=1.0.0
